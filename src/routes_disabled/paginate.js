import { paginate } from 'mythic/paginate'
import { p } from 'mythic/markup'
// import { compose, map, inc, length, range, init, append } from 'ramda'

let el = node => p('pending')

export default el
